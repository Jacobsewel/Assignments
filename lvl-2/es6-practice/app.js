/* ### **Task 1**

Re-write this `.map()` using an arrow function:

Be aware that if JavaScript sees a `{` directly after the `=>` it will think it's starting a function,
 and not starting an object, so the `:` will be an unexpected symbol. */

/*const apple = ["bright red", "ripe", "rotten"]
function mapFruit(arr) {
    const newArray = arr.map()
    return apple.map(apple) => {
        return { type: "apple", name: apple }
    })
}
const fruit = arr => arr.map(item => ({type: "apple", name: item }) )

    console.log(fruit(apple))

 Re-write this .filter() ’s callback function using an arrow function: */




/*Task 2

Re-write this `.filter()` ’s callback function using an arrow function:*/

/*const people = [
    {
        name: "Princess Peach",
        friendly: false
    },
    {
        name: "Luigi",
        friendly: true
    },
    {
        name: "Mario",
        friendly: true
    },
    {
        name: "Bowser",
        friendly: false
    }
]

function filterForFriendly(arr) {
    return arr.filter(function(person) {
        return person.friendly
    })
}

const nintendoCharacters = people.filter(person => friendly.name === true)

console.log(nintendoCharacters)*/

/*Task 3 Re-write the following functions to be arrow functions:*/

const doMathSum = (a, b) =>  a + b



const produceProduct = (a, b) => a * b











